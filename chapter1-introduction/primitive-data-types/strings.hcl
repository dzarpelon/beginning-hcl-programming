# Strings are created with double quotes ("text")
hello = "Hello World"
# To create multi-line strings we need a delimiter identifier (it can be any, in the example below it's 'EOF' but we need to start with <<:

<< EOF
Hello
HCL
EOF